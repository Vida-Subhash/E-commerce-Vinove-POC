import { FilterbycategoriesPipe } from './filterbycategories.pipe';

describe('FilterbycategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
